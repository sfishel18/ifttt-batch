name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: node:10-alpine

    steps:
      - uses: actions/checkout@v1

      - name: Install dependencies
        run: npm install
